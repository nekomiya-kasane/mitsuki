# T7a1.4.1 — Halo / Gap Lines

**Phase**: 11-7a1
**Component**: Halo / Gap Lines
**Status**: Complete
**Current Step**: Done
**Resume Hint**: All steps complete. Halo/gap logic in fragment shader, config fields in DisplayStyleEdgeConfig, depth texture bindings in EdgeRenderer, tests in test_edge_sdf_render.cpp.
**Effort**: M (1-2h)

## Dependencies

| Task ID | Name | Status | Key Output Used |
|---------|------|--------|-----------------|
| T7a1.3.1 | SDF Edge Render Pipeline | Not Started | `EdgeRenderer`, SDF fragment shader |

## Context Anchor

### This Task's Contract

**Produces**: T-junction clarity enhancement in SDF edge fragment shader.

- **Algorithm** (per arch spec §5.9.4): Per edge fragment, sample depth in perpendicular direction (±haloWidth px). If crossing edge is closer → discard (gap). If this edge is closer → render white halo margin.
- **Cost**: 2 extra depth samples per edge fragment, ~0.1ms @1M edges.
- **Purpose**: Technical illustration quality — prevents confusing intersections where edges cross.

## Steps

- [x] **Step 1**: Update DisplayStyleEdgeConfig struct (add haloWidth, haloColor) + hlr_common.slang mirror
      `[verify: compile]`
- [x] **Step 2**: Add halo/gap logic to SDF fragment shader (perpendicular depth sampling, gap discard, halo margin)
      `[verify: compile]`
- [x] **Step 3**: Update EdgeRenderer bindings (depth texture binding 4, sampler binding 5) + Render() signature
      `[verify: compile]`
- [x] **Step 4**: Tests (config presets, halo parameters, GPU no-crash with halo enabled)
      `[verify: test]`
