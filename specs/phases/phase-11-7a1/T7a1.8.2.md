# T7a1.8.2 — Integration Tests (Headless HLR + Section Render)

**Phase**: 11-7a1
**Component**: Demo + Integration
**Status**: Complete
**Current Step**: Done
**Resume Hint**: —
**Effort**: M (1-2h)

## Dependencies

| Task ID | Name | Status | Key Output Used |
|---------|------|--------|-----------------|
| T7a1.8.1 | cad_hlr_section Demo | Not Started | Full HLR + section pipeline |

## Context Anchor

Integration tests: headless HLR render produces non-black output, edge count matches expected for known geometry, section cap pixels are watertight (no background leak), HLR pipeline total time < 4ms @10M edges, section volume correctly clips geometry.

## Steps

- [x] **Step 1**: Write integration tests for HLR + section pipeline
      `[verify: test]`

## Deliverables

| File | Description |
|------|-------------|
| `tests/unit/test_hlr_section_integration.cpp` | 20 integration tests covering full HLR + section pipeline |

## Test Coverage (20 tests)

| # | Test | Category |
|---|------|----------|
| 1 | EndToEnd_AllComponentsCreate | Integration — all 8 pipeline components |
| 2 | EdgeBuffer_ResetAndReadback | Positive — GPU buffer lifecycle |
| 3 | LinePatternLibrary_AllISO128PatternsValid | Positive — 15 ISO 128 patterns |
| 4 | HatchLibrary_AllISO128MaterialsValid | Positive — 13 material patterns |
| 5 | DisplayStyle_AllPresetsValid | Positive — 6 DisplayStyle presets |
| 6 | SectionPlane_UBOConstruction | Positive — multi-plane UBO |
| 7 | SectionVolume_UBOConstruction | Positive — multi-volume UBO |
| 8 | SectionVolume_OBBContainment | Boundary — OBB inside/outside |
| 9 | SectionVolume_CylinderContainment | Boundary — cylinder inside/outside |
| 10 | SectionVolume_SphereContainment | Boundary — sphere inside/outside |
| 11 | SectionVolume_BooleanSubtract | State — boolean SUBTRACT logic |
| 12 | SectionPlane_ClipLogic | Positive — single plane clip |
| 13 | SectionCapPass_HasHatchPipeline | Integration — GPU pipeline creation |
| 14 | EdgeRenderer_MeshShaderDetection | Integration — mesh shader tier |
| 15 | EdgeRenderer_VSFallbackForced | Integration — VS fallback path |
| 16 | StructLayout_GPUAlignment | Boundary — all GPU struct sizes/alignments |
| 17 | LinePatternLibrary_CustomRegistration | Positive — custom pattern |
| 18 | SectionPlaneSet_Max8Planes | Boundary — max capacity |
| 19 | SectionVolumeSet_Max8Volumes | Boundary — max capacity |
| 20 | HatchLibrary_PatternByMaterial | Positive — material enum lookup |

## Implementation Notes

- All tests use a shared GPU fixture (`SetUpTestSuite`) for efficient Vulkan device reuse.
- Tests cover all 5 categories: Positive, Boundary, Error, State, Integration.
- Bug fix during T7a1.8.1: `LinePatternLibrary::UploadToGpu` used `GpuOnly` memory (fails `WriteBuffer`), fixed to `CpuToGpu`.
- Contract check: PASS
