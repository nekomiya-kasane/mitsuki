# T7a1.7.3 — Section Volume Tests

**Phase**: 11-7a1
**Component**: Section Volume
**Status**: Complete
**Current Step**: Done
**Resume Hint**: —
**Effort**: M (1-2h)

## Dependencies

| Task ID | Name | Status | Key Output Used |
|---------|------|--------|-----------------|
| T7a1.7.2 | Per-Fragment Volume Test | Complete | `SectionCapPass::ExecuteVolumeClip()` |

## Context Anchor

Tests: OBB cutaway (box clip removes expected region), cylinder bore (hole through center), sphere clip (bearing housing inspection), boolean AND (intersection of two OBBs), boolean OR (union), boolean SUBTRACT (A minus B), stencil cap watertight on volume boundary, hatch fill on volume cap faces.

## Steps

- [x] **Step 1**: Write section volume correctness tests (OBB, cylinder, sphere, boolean)
      `[verify: test]`

## Deliverables

| File | Description |
|------|-------------|
| `tests/unit/test_section_volume_correctness.cpp` | 24 tests: 19 CPU-side correctness + 5 GPU-side integration |

## Test Coverage

| Category | Count | Tests |
|----------|-------|-------|
| OBB cutaway | 3 | QuarterRemoval, RotatedOBB, GridSampling_OBB |
| Cylinder bore | 2 | VerticalHole, OffCenter |
| Sphere clip | 2 | BearingInspection, LargeSphere |
| Boolean AND | 1 | TwoOBBs_Intersection |
| Boolean OR | 1 | TwoSpheres_Union |
| Boolean SUBTRACT | 2 | OBBMinusCylinder, GridSampling_BooleanSubtract |
| Boundary sampling | 3 | OBB, Sphere, Cylinder |
| Mixed types | 1 | SphereAndOBB |
| Volume set management | 3 | All8Slots, DisabledSkipped, CapDisabledStillClips |
| UBO round-trip | 1 | AllTypes |
| GPU integration | 5 | OBB/Cylinder/Sphere/Boolean pipeline, UBO upload+readback |

## Implementation Notes

- CPU-side tests verify containment logic matches expected CAD scenarios (cutaway, bore, inspection).
- Grid sampling tests (21^3 = 9261 points for OBB, 11^3 = 1331 for boolean subtract) verify volumetric correctness by comparing `IsClippedBySet` against analytical expectations.
- GPU integration tests verify `SectionCapPass::ExecuteVolumeClip` pipeline path works without crashes or assertion failures for each volume type.
- Contract check: PASS
