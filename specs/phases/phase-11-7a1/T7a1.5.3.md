# T7a1.5.3 — Contour Extraction Compute

**Phase**: 11-7a1
**Component**: Section Plane v2
**Status**: Complete
**Current Step**: —
**Resume Hint**: —
**Effort**: M (1-2h)

## Dependencies

| Task ID | Name | Status | Key Output Used |
|---------|------|--------|-----------------|
| T7a1.5.2 | Multi-Plane Clip + Stencil | Complete | Section plane clip geometry |

## Context Anchor

Compute shader extracts intersection polyline where section plane cuts mesh geometry. Output: `ContourSegment[]` SSBO (polyline segments). Used for: animated section sweep visualization, 2D drawing export (Phase 15a).

## Deliverables

| File | Description |
|------|-------------|
| `include/miki/hlr/ContourExtractor.h` | `ContourSegment` (32B GPU struct) + `ContourExtractor` class |
| `src/miki/hlr/ContourExtractor.cpp` | GPU pipeline creation, Extract dispatch, CPU `IntersectTrianglePlane()` |
| `shaders/hlr/section_contour_extract.slang` | Per-triangle classify + intersect compute shader (256 threads/WG) |
| `tests/unit/test_contour_extraction.cpp` | 12 tests (8 CPU + 4 GPU) |

## Steps

- [x] **Step 1**: Implement contour extraction compute shader + ContourExtractor class
      `[verify: compile]`
- [x] **Step 2**: Tests (12 tests: CPU intersection correctness + GPU readback count validation)
      `[verify: test — 12/12 pass]`
