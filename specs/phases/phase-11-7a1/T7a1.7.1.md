# T7a1.7.1 — Section Volume Types (OBB / Cylinder / Sphere / Boolean)

**Phase**: 11-7a1
**Component**: Section Volume
**Status**: Complete
**Current Step**: Done
**Resume Hint**: —
**Effort**: L (< 1h)

## Dependencies

| Task ID | Name | Status | Key Output Used |
|---------|------|--------|-----------------|
| T7a1.5.1 | Section Plane Types | Complete | `SectionPlaneConfig` (reused for volume boundary planes) |

## Context Anchor

### This Task's Contract

**Produces**:

| File | Exposure | Ref Heat | Defines |
|------|----------|----------|---------|
| `include/miki/hlr/SectionVolume.h` | **public** | **M** | `SectionVolume`, `VolumeType`, `VolumeParams`, `SectionVolumeGpu`, `SectionVolumeUBO` |

- `VolumeType`: `enum class { OBB, Cylinder, Sphere, Boolean }`.
- `VolumeParams`: fixed-size union (64B, alignas(16)) — OBB (center + 3 axes with interleaved half-extents), Cylinder (axis + center + radius + halfHeight), Sphere (center + radius).
- `SectionVolumeGpu`: 80B GPU struct (type + enabled + capEnabled + pad + params).
- `SectionVolumeUBO`: 656B GPU UBO (8 volumes + activeMask + volumeCount + pad).
- `SectionVolume::CreateOBB/CreateCylinder/CreateSphere/CreateBoolean` → factory methods.
- `SectionVolume::Contains(x,y,z)` → CPU-side containment test.
- Boolean: AND (intersection), OR (union), SUBTRACT (A minus B).
- Sphere: `length(worldPos - center) < radius`.
- Per arch spec: "per-fragment inside-volume test via push-constant volume params".

### Downstream Consumers

- T7a1.7.2: per-fragment volume test implementation.
- Phase 7a-2: combined with OIT transparency.

## Steps

- [x] **Step 1**: Define SectionVolume.h with VolumeType (OBB/Cylinder/Sphere/Boolean) / VolumeParams
      `[verify: compile]`

## Deliverables

| File | Type | Description |
|------|------|-------------|
| `include/miki/hlr/SectionVolume.h` | Header | VolumeType, BooleanVolumeOp, OBBParams, CylinderParams, SphereParams, BooleanParams, VolumeParams, SectionVolumeGpu, SectionVolume, SectionVolumeSet, SectionVolumeUBO |
| `src/miki/hlr/SectionVolume.cpp` | Source | Factory methods, CPU containment tests (OBB/Cylinder/Sphere), Boolean evaluation, SectionVolumeSet, BuildSectionVolumeUBO |
| `tests/unit/test_section_volume.cpp` | Test | 39 tests covering struct layout, factory types, OBB/Cylinder/Sphere containment (center/inside/outside/boundary/rotated), Boolean AND/OR/SUBTRACT, VolumeSet management, UBO build |

## Implementation Notes

- OBB layout packs center (float3+pad), then each axis with its half-extent interleaved (axisX+halfExtentX, axisY+halfExtentY, axisZ+halfExtentZ) — GPU-friendly float4 rows
- CPU containment: OBB uses dot-product projection onto local axes; Cylinder uses axial projection + radial distance; Sphere uses distance squared
- Boolean evaluated via SectionVolumeSet::IsClippedBySet which looks up child volumes by index
- All structs have static_assert for sizeof, alignof, offsetof, trivially_copyable
- Contract check: PASS
