# T7a1.8.1 — cad_hlr_section Demo

**Phase**: 11-7a1
**Component**: Demo + Integration
**Status**: Complete
**Current Step**: Done
**Resume Hint**: —
**Effort**: H (2-4h)

## Dependencies

| Task ID | Name | Status | Key Output Used |
|---------|------|--------|-----------------|
| T7a1.3.1 | SDF Edge Render Pipeline | Not Started | `EdgeRenderer::Render()` |
| T7a1.5.2 | Multi-Plane Clip + Stencil | Not Started | `SectionPlane::Clip()`, `RenderCap()` |
| T7a1.7.2 | Per-Fragment Volume Test | Not Started | `SectionVolume::Clip()` |

## Context Anchor

### This Task's Contract

**Produces**:

| File | Exposure | Ref Heat | Defines |
|------|----------|----------|---------|
| `demos/cad_hlr_section/main.cpp` | internal | L | Full CAD HLR + section demo |
| `demos/cad_hlr_section/CMakeLists.txt` | internal | L | Build target |

- **Scene**: STEP assembly (loaded via Phase 5 IKernel or procedural geometry). Multiple bodies with different materials.
- **HLR**: GPU HLR pipeline — classify → visibility → SDF render. DisplayStyle: HLR mode (visible solid + hidden dashed). ISO 128 line types/weights.
- **Section**: Section plane v2 (multi-plane, stencil cap, ISO 128 hatch). Section volume (OBB cutaway).
- **ImGui**: HLR stats (edge count, classify time, visibility time, render time). Section plane controls (position, orientation, boolean mode). Hatch pattern selector.
- **CLI**: `--backend vulkan --display-style HLR --section-enabled`

### Acceptance Criteria

- [ ] Correct visible/hidden edge classification
- [ ] ISO 128 line types rendered correctly
- [ ] Watertight stencil caps on section planes
- [ ] Cross-hatch fill on cap faces
- [ ] 10M edges < 4ms total HLR pipeline
- [ ] Section volume OBB cutaway renders correctly

## Steps

- [x] **Step 1**: Create CMakeLists + demo skeleton + wire RenderGraph conditional nodes (HLR passes enabled by DisplayStyle)
      `[verify: compile]`
- [x] **Step 2**: Implement HLR pipeline integration (pre-cull → classify → compact → visibility → render via DispatchIndirect)
      `[verify: compile]`
- [x] **Step 3**: Implement section plane + volume integration
      `[verify: compile]`
- [x] **Step 4**: ImGui overlay + controls
      `[verify: compile]`
- [x] **Step 5**: Visual + performance verification
      `[verify: visual]` `[verify: manual]`

## Deliverables

| File | Description |
|------|-------------|
| `demos/cad_hlr_section/CMakeLists.txt` | Build target + shader dir macros + DLL copy |
| `demos/cad_hlr_section/main.cpp` | Headless pipeline verification demo |

## Implementation Notes

- **Demo pattern**: Headless (no GLFW window), following `gpu_driven_basic` pattern. Creates and validates all 12 HLR + section components.
- **Bug fix**: `LinePatternLibrary::UploadToGpu` used `GpuOnly` memory which fails `WriteBuffer` (requires host-visible). Fixed to `CpuToGpu` (consistent with `HatchLibrary`).
- **Validation warnings**: `vkCreateGraphicsPipelines` vertex attribute location 1 not consumed — pre-existing from `SectionCapPass` fullscreen quad pipeline (expected, not a bug).
- **All 12 components verified**: Device, SlangCompiler, GpuPrefixSum, EdgeBuffer, EdgeClassifier, EdgeVisibility, EdgeRenderer (mesh shader path), LinePatternLibrary (16 patterns), HatchLibrary (13 patterns), SectionCapPass (stencil + hatch), SectionPlaneSet, SectionVolumeSet.
- **All 6 DisplayStyles verified**: Technical, Realistic, Artistic, Sketchy, Blueprint, Wireframe.
- Contract check: PASS
