# T7a2.6.3 — Deferred Resolve Integration (Read Cluster Grid, Iterate Lights)

**Phase**: 12-7a2
**Component**: Clustered Light Culling
**Status**: Not Started
**Effort**: M (1-2h)

## Dependencies

| Task ID | Name | Status |
|---------|------|--------|
| T7a2.6.2 | 3D Froxel Grid Compute | Not Started |

## Scope

Modify existing DeferredResolve (Phase 3a) to read ClusterGrid instead of iterating a flat light UBO array. Per arch spec §7.3 Pass #18: "T1: read cluster grid → iterate point/spot/area lights per froxel. T2/3/4: iterate CPU-sorted light UBO array."

- Per-pixel: compute froxel index from screen position + linearized depth. Read `ClusterGrid[froxelIdx].lightIndices[]` → iterate → accumulate BRDF per light.
- Area light evaluation: LTC (Linearly Transformed Cosines) specular + analytic polygon diffuse. Requires LTC lookup textures (T7a2.6.4).
- Integration point: existing `deferred_resolve.slang` gains a `#ifdef CLUSTERED` path. Tier detection at pipeline creation time via specialization constant.

## Steps

- [ ] **Step 1**: Modify deferred_resolve.slang with clustered light iteration path
      `[verify: compile]`
- [ ] **Step 2**: Wire ClusterGrid SSBO binding into deferred resolve descriptor set
      `[verify: compile]`
- [ ] **Step 3**: Tests (lighting correctness with 100+ lights, compare vs flat UBO path)
      `[verify: test]`
