# T7a2.2.4 — Multi-Hit Drill Pick + Topology-Level Return

**Phase**: 12-7a2
**Component**: Ray Picking v2 — BLAS/TLAS
**Status**: Not Started
**Effort**: M (1-2h)

## Dependencies

| Task ID | Name | Status |
|---------|------|--------|
| T7a2.2.3 | Single-Point Pick | Not Started |

## Scope

Extend drill pick with multi-hit collection (all intersections along ray, depth-sorted). Topology-level return path: `instanceId → GpuInstance.entityId → Entity → TopoGraph::PrimitiveToFace(entityId, triIdx) → FaceId → FaceToEdges() → EdgeIds → FaceToVertices() → VertexIds`. `PickLevel` truncation: Part = entity only, Body = entity + body, Face = + faceId, Edge = + edgeIds, Vertex = + vertexIds. Per arch spec §5.5: "Full topology path from single pixel (no CPU re-intersection)".

## Steps

- [ ] **Step 1**: Implement multi-hit collection + depth sort in HitBuffer
      `[verify: compile]`
- [ ] **Step 2**: Implement topology-level return via TopoGraph resolution
      `[verify: compile]`
- [ ] **Step 3**: Tests (multi-hit order correctness, PickLevel truncation, topology resolution)
      `[verify: test]`
