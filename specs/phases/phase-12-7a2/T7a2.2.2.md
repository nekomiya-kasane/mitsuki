# T7a2.2.2 — Incremental TLAS (Update Moved Instances)

**Phase**: 12-7a2
**Component**: Ray Picking v2 — BLAS/TLAS
**Status**: Not Started
**Effort**: M (1-2h)

## Dependencies

| Task ID | Name | Status |
|---------|------|--------|
| T7a2.2.1 | Incremental BLAS | Not Started |

## Scope

Incremental TLAS rebuild when instances are added/removed/moved. Uses `VK_BUILD_ACCELERATION_STRUCTURE_MODE_UPDATE_KHR` for in-place update when only transforms change (<0.1ms). Full rebuild when topology changes. Per arch spec §4.2 Step 2: "BLAS/TLAS update recorded in same command buffer as geometry rendering". Frame-consistency invariant: TLAS reflects same geometry version as displayed VisBuffer.

## Steps

- [ ] **Step 1**: Implement TLAS create/update/rebuild API
      `[verify: compile]`
- [ ] **Step 2**: Tests (transform update, instance add/remove, frame consistency)
      `[verify: test]`
