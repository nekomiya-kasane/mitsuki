# T7a2.2.1 — Incremental BLAS (Refit Dirty + Periodic Compact)

**Phase**: 12-7a2
**Component**: Ray Picking v2 — BLAS/TLAS
**Status**: Not Started
**Effort**: H (2-4h)

## Scope

Incremental BLAS management: refit dirty bodies (<0.2ms), periodic compact (every 60 frames). Uses `VK_KHR_acceleration_structure`. Refit = update AABBs in-place without rebuild. Compact = defragment AS memory. Per arch spec: "BLAS/TLAS updates recorded in same command buffer as geometry rendering pass".

## Steps

- [ ] **Step 1**: BLAS create/refit/compact API + Vulkan implementation
      `[verify: compile]`
- [ ] **Step 2**: Tests (refit correctness, compact memory reduction)
      `[verify: test]`
