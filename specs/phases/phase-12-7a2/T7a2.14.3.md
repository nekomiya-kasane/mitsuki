# T7a2.14.3 — Integration Tests (All Subsystems)

**Phase**: 12-7a2
**Component**: Demo + Integration
**Status**: Not Started
**Effort**: H (2-4h)

## Dependencies

| Task ID | Name | Status |
|---------|------|--------|
| T7a2.14.2 | cad_hlr_oit Combined Demo | Not Started |

## Scope

Comprehensive integration test suite validating all Phase 7a-2 subsystems work together:

## Tests

| Test Name | Category | Validates |
|-----------|----------|-----------|
| `OIT_HeadlessRender` | Integration | OIT insert + resolve produces non-black transparent output |
| `OIT_SortCorrectness_16Layers` | Positive | 16 overlapping transparent layers sorted front-to-back |
| `OIT_PoolOverflow` | Boundary | Pool overflow handled gracefully (no crash, fallback to weighted) |
| `Pick_PointNoDrill_Headless` | Integration | VisBuffer readback returns valid instanceId |
| `Pick_DrillMultiHit` | Integration | RT ray query returns depth-ordered multi-hit |
| `Pick_BoxDrillFindsOccluded` | Integration | Drill box finds 100%-occluded instances |
| `Pick_LassoConcavePolygon` | Integration | Concave polygon mask correct |
| `ClusteredLight_1KLights` | Integration | 1K lights correctly assigned to froxels |
| `ShadowAtlas_TileAllocation` | Integration | Tiles allocated and LRU evicted correctly |
| `SSR_ReflectionQuality` | Integration | SSR reflection matches IBL within tolerance |
| `DoF_CoCAccuracy` | Integration | CoC values correct at known depths |
| `MotionBlur_DirectionAccuracy` | Integration | Blur direction matches velocity vector |
| `CAS_SharpeningMetric` | Integration | Sharpened image has higher edge contrast |
| `ColorGrade_IdentityLUT` | Integration | Identity LUT produces no change |
| `RTAO_QualityVsGTAO` | Integration | RTAO produces similar AO to GTAO baseline |
| `Explode_IdentityAtT0` | Integration | Explode at t=0 produces identity transform |
| `MaterialGraph_NoiseNode` | Integration | Noise material compiles and renders |
| `Perf_FullPipeline_60fps` | Benchmark | Complete pipeline >= 60fps on RTX 4070 |

## Steps

- [ ] **Step 1**: Write all integration tests
      `[verify: test]`
