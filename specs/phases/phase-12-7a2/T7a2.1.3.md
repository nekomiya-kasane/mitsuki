# T7a2.1.3 — OIT Resolve Pass (Compute Sort + Composite)

**Phase**: 12-7a2
**Component**: Linked-List OIT
**Status**: Not Started
**Effort**: H (2-4h)

## Dependencies

| Task ID | Name | Status |
|---------|------|--------|
| T7a2.1.2 | OIT Insert Pass | Not Started |

## Scope

Compute shader: per-pixel read linked list, sort by depth (insertion sort ≤16 fragments, merge sort >16). Front-to-back alpha composite. Output: RGBA16F composited transparent color. Sentinel pixels (head == 0xFFFFFFFF) = fully opaque, skip.

## Steps

- [ ] **Step 1**: Implement OIT resolve compute shader (sort + composite)
      `[verify: compile]`
- [ ] **Step 2**: Tests (sort correctness 1/4/8/16/64 layers, composite accuracy)
      `[verify: test]`
