# T5.9.1 — IGpuGeometry Interface

**Phase**: 07-5 (ECS & Scene)
**Component**: IGpuGeometry
**Roadmap Ref**: `roadmap.md` L1575 — GPU compute API stubs for geometry kernels (9 operations)
**Status**: Complete
**Current Step**: Done
**Resume Hint**: —
**Effort**: M (1-2h)

## Dependencies

| Task ID | Name | Status | Key Output Used |
|---------|------|--------|-----------------|
| T5.8.1 | IKernel + SimKernel + KernelFactory | Not Started | `IKernel`, `ShapeId`, kernel types |

## Context Anchor

### This Task's Contract

**Produces**:

| File | Exposure | Ref Heat | Defines |
|------|----------|----------|---------|
| `include/miki/kernel/IGpuGeometry.h` | **public** | **M** | `IGpuGeometry` Pimpl interface with 9 stub operations |
| `src/miki/kernel/GpuGeometryStub.cpp` | internal | L | All methods return `NotImplemented` |
| `tests/unit/test_gpu_geometry.cpp` | internal | L | Smoke tests |

- **Error model**: all operations return `Result<T>` with `ErrorCode::NotImplemented` until downstream phases activate them
- **Thread safety**: Pimpl-based, single-owner
- **Invariants**: calling any stub operation returns `NotImplemented` cleanly (no crash, no UB)

### Downstream Consumers

- `IGpuGeometry.h` (**public**, heat **M**):
  - Phase 7b: activates `GpuTessellate`, `GpuBooleanPreview`, `GpuMinDistance`, `GpuDraftAngle`, `GpuMassProperties`
  - Phase 9: activates `GpuInterference`, `GpuCurvature`
  - Phase 6b: activates `GpuSimplify` (QEM)

### Upstream Contracts

- T5.8.1: `ShapeId`, `FaceId` from `KernelTypes.h`
  - Source: `include/miki/kernel/KernelTypes.h`
- Phase 06-4: `BDAPointer` from `BDAManager`
  - Source: `include/miki/resource/BDAPointer.h`

### Technical Direction

- **Pimpl stubs only**: Phase 5 declares the interface. All methods return `NotImplemented`. Each downstream phase implements specific methods by replacing the stub in the Pimpl.
- **9 operations**: GpuTessellate, GpuBooleanPreview, GpuInterference, GpuMinDistance, GpuDraftAngle, GpuCurvature, GpuSimplify, GpuMassProperties, GpuSolveConstraints (reserved).
- **Thin wrapper pattern**: each method is a thin dispatch to internal compute pipeline. Host kernel calls `IGpuGeometry`, miki handles GPU resource lifecycle.

## Files

| Action | Path | Exposure | Ref Heat | Notes |
|--------|------|----------|----------|-------|
| Create | `include/miki/kernel/IGpuGeometry.h` | **public** | **M** | 9 stub operations |
| Create | `src/miki/kernel/GpuGeometryStub.cpp` | internal | L | NotImplemented returns |
| Create | `tests/unit/test_gpu_geometry.cpp` | internal | L | Smoke tests |

## Steps

- [ ] **Step 1**: Define IGpuGeometry interface with 9 operations
      **Files**: `IGpuGeometry.h` (**public** M)

      **Signatures**:

      | Symbol | Signature | Attrs |
      |--------|-----------|-------|
      | `IGpuGeometry::GpuTessellate` | `(BufferHandle nurbsSSBO, TextureHandle trimSdf) -> Result<BufferHandle>` | `[[nodiscard]]` virtual |
      | `IGpuGeometry::GpuBooleanPreview` | `(ShapeId a, ShapeId b, BooleanType) -> Result<TextureHandle>` | `[[nodiscard]]` virtual |
      | `IGpuGeometry::GpuInterference` | `(BufferHandle blasA, BufferHandle blasB) -> Result<std::vector<std::pair<uint32_t,uint32_t>>>` | `[[nodiscard]]` virtual |
      | `IGpuGeometry::GpuMinDistance` | `(BufferHandle blasA, BufferHandle blasB) -> Result<double>` | `[[nodiscard]]` virtual |
      | `IGpuGeometry::GpuDraftAngle` | `(BufferHandle meshHandle, float3 pullDir) -> Result<BufferHandle>` | `[[nodiscard]]` virtual |
      | `IGpuGeometry::GpuCurvature` | `(BufferHandle meshHandle) -> Result<BufferHandle>` | `[[nodiscard]]` virtual |
      | `IGpuGeometry::GpuSimplify` | `(BufferHandle meshHandle, uint32_t targetCount) -> Result<BufferHandle>` | `[[nodiscard]]` virtual |
      | `IGpuGeometry::GpuMassProperties` | `(BufferHandle meshHandle) -> Result<MassProps>` | `[[nodiscard]]` virtual |
      | `IGpuGeometry::GpuSolveConstraints` | `(/* reserved */) -> Result<void>` | `[[nodiscard]]` virtual |
      | `~IGpuGeometry` | virtual default | — |

      Default implementations all return `ErrorCode::NotImplemented`.

      **Acceptance**: compiles on both build paths
      `[verify: compile]`

- [ ] **Step 2**: Implement stub + tests
      **Files**: `GpuGeometryStub.cpp` (internal L), `test_gpu_geometry.cpp` (internal L)
      **Acceptance**: all tests pass
      `[verify: test]`

## Tests

| Test Name | Category | Validates | Steps |
|-----------|----------|-----------|-------|
| `TEST(IGpuGeometry, GpuTessellateReturnsNotImplemented)` | Positive | stub returns NotImplemented | 1-2 |
| `TEST(IGpuGeometry, GpuBooleanReturnsNotImplemented)` | Positive | stub returns NotImplemented | 1-2 |
| `TEST(IGpuGeometry, GpuMinDistanceReturnsNotImplemented)` | Positive | stub returns NotImplemented | 1-2 |
| `TEST(IGpuGeometry, GpuSimplifyReturnsNotImplemented)` | Positive | stub returns NotImplemented | 1-2 |
| `TEST(IGpuGeometry, GpuMassPropertiesReturnsNotImplemented)` | Positive | stub returns NotImplemented | 1-2 |
| `TEST(IGpuGeometry, AllStubsReturnNotImplemented)` | **Integration** | loop all 9 methods, verify NotImplemented | 1-2 |

## Design Decisions

*(Fill during implementation)*

## Implementation Notes

*(Fill during implementation)*
